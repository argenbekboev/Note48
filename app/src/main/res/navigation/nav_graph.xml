<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/notesFragment">

    <fragment
        android:id="@+id/notesFragment"
        android:name="com.example.note.presentation.ui.fragments.notes.NotesFragment"
        android:label="fragment_first">

        <action
            android:id="@+id/action_notesFragment_to_add_edit_notesFragment"
            app:destination="@id/add_edit_notesFragment" />
    </fragment>

    <fragment
        android:id="@+id/add_edit_notesFragment"
        android:name="com.example.note.presentation.ui.fragments.add_edit.AddEditNotesFragment"
        android:label="fragment_second" >

    <argument
        android:name="note"
        android:defaultValue="null"
        app:argType="com.example.note.domain.model.Note"/>
    </fragment>

</navigation>